<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QCP-Omics Report</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        thead th {
            position: sticky;
            top: 0;
        }
    </style>
</head>
<body>
<div class="container my-5">
    <h1 class="text-center mb-4 fs-2">QCP-Omics Report</h1>
    {% for i in range(steps | length) %}
        <div class="mb-5">
            <h2 class="text-primary fs-3">{{ i + 1 }}. {{ steps[i].step | replace("_", " ") | capitalize }}</h2>
            <p>{{ steps[i].info }}</p>

            {% if steps[i].output %}
                <h3 class="fs-4">Output</h3>
                <ol class="list-group list-group-numbered overflow-auto" style="max-height: 400px;">
                    {% for k, v in steps[i].output.items() %}
                        <li class="list-group-item d-flex justify-content-between align-items-start">
                            <div class="ms-2 me-auto">
                                <div class="fw-bold">{{ k }}</div>
                                <ul class="list-group list-group-flush">
                                    {% for j in range(v | length) %}
                                        <li class="list-group-item"><u>{{ v[j][0] }}</u>: {{ v[j][1] }}</li>
                                    {% endfor %}
                                </ul>
                            </div>
                            <span class="badge text-bg-primary rounded-pill">{{ v | length }}</span>
                        </li>
                    {% endfor %}
                </ol>

            {% endif %}

            {% if steps[i].data is not none %}
                <h3 class="fs-4">Data</h3>
                <div class="table-responsive overflow-auto" style="max-height: 400px;">
                    {{ steps[i].data.to_html(classes="table table-striped table-bordered table-hover") | safe }}
                </div>
            {% endif %}
        </div>
        <hr>
    {% endfor %}
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
