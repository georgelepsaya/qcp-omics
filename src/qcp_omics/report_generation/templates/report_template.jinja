<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QCP-Omics Report</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container my-5">
        <h1 class="text-center mb-4">QCP-Omics Report</h1>

        {% for step in steps %}
        <div class="mb-5">
            <h2 class="text-primary">{{ step.step }}</h2>
            <p>{{ step.info }}</p>

            {% if step.output %}
            <h3>Output</h3>
            <pre class="bg-light border rounded p-3overflow-auto" style="max-height: 400px;">{{ step.output | tojson(indent=4) | safe }}</pre>
            {% endif %}

            {% if step.data is not none %}
            <h3>Data</h3>
            <div class="table-responsive overflow-auto" style="max-height: 400px;">
                {{ step.data.to_html(classes="table table-striped table-bordered table-hover") | safe }}
            </div>
            {% endif %}
        </div>
        <hr>
        {% endfor %}
    </div>
    <!-- Bootstrap JS (Optional for interactive components) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
