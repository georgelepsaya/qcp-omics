<div class="mb-5">
    <h2 class="fs-5">{{ i + 1 }}. {{ steps[i].step | replace("_", " ") | capitalize }}</h2>
    <hr class="mt-0 mb-3">
<p>
    The table below shows how many values (in %) are missing for each feature of the dataset.
</p>
    <ol class="list-group list-group-numbered overflow-auto" style="max-height: 600px;">
        {% for k, v in steps[i].output.items() %}
            <li class="list-group-item d-flex justify-content-between align-items-start">
                <div class="ms-2 me-auto">
                    <div class="fw-bold">{{ k }}</div>
                    {{ v | round(2) }}%
                </div>
            </li>
        {% endfor %}
    </ol>
</div>